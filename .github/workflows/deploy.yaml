name: Wake EyeOS

on:
  schedule:
    - cron: '0 */4 * * *'  # every 4 hours
  workflow_dispatch:       # allows manual trigger

jobs:
  wake:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Wake EyeOS
        run: |
          echo "👁️ EyeOS wake-up triggered at $(date)"
